# -- Runtime ------------------------------------------------------------------
pandas>=2.0.0
pyarrow>=14.0.0
pydantic[email]>=2.0.0
pyyaml>=6.0

# -- Testing ------------------------------------------------------------------
pytest>=7.4.0
pytest-cov>=4.1.0

# -- Linting & formatting (ruff replaces flake8 + isort + pyupgrade) ----------
ruff>=0.4.0

# -- Static type checking -----------------------------------------------------
mypy>=1.10.0
pandas-stubs>=2.0.0
types-PyYAML>=6.0.12
